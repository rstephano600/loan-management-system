
composer require realrashid/sweet-alert;



<!-- 
<button onclick="confirmDelete({{ $item->id }})" class="btn btn-danger">
    Delete
</button>

<button onclick="confirmApprove({{ $item->id }})" class="btn btn-success">
    Approve
</button>

<button onclick="confirmAccept({{ $item->id }})" class="btn btn-info">
    Accept
</button>
<button onclick="confirmDelete('{{ route('items.destroy',$item->id) }}', {{ $item->id }})">
-->
